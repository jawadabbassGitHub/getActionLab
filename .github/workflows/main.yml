name: First workflow lab

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print a welcome message
        run: echo "Welcome to my first GitHub Actions workflow"

      - name: List repository files
        run: ls -la
